"""Testing setup for {{ package_name }}."""
from plone.app.testing import FunctionalTesting
from plone.app.testing import IntegrationTesting
from plone.app.testing import PloneSandboxLayer
from plone.app.testing import SITE_OWNER_NAME
from plone.app.testing import SITE_OWNER_PASSWORD
from plone.testing.zope import WSGI_SERVER_FIXTURE

import {{ package_name }}


class {{ package_name.replace('.', ' ').title().replace(' ', '') }}Layer(PloneSandboxLayer):
    """Custom testing layer for {{ package_name }}."""

    def setUpZope(self, app, configurationContext):
        """Set up Zope."""
        self.loadZCML(package={{ package_name }})

    def setUpPloneSite(self, portal):
        """Set up Plone site."""
        self.applyProfile(portal, "{{ package_name }}:default")


FIXTURE = {{ package_name.replace('.', ' ').title().replace(' ', '') }}Layer()

INTEGRATION_TESTING = IntegrationTesting(
    bases=(FIXTURE,),
    name="{{ package_name.replace('.', ' ').title().replace(' ', '') }}Layer:IntegrationTesting",
)

FUNCTIONAL_TESTING = FunctionalTesting(
    bases=(FIXTURE,),
    name="{{ package_name.replace('.', ' ').title().replace(' ', '') }}Layer:FunctionalTesting",
)

ACCEPTANCE_TESTING = FunctionalTesting(
    bases=(FIXTURE, WSGI_SERVER_FIXTURE),
    name="{{ package_name.replace('.', ' ').title().replace(' ', '') }}Layer:AcceptanceTesting",
)


# Test credentials
TEST_USER_ID = "testuser"
TEST_USER_NAME = "testuser"
SITE_OWNER_NAME = SITE_OWNER_NAME
SITE_OWNER_PASSWORD = SITE_OWNER_PASSWORD
