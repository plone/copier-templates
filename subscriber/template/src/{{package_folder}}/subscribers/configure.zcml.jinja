<configure
    xmlns="http://namespaces.zope.org/zope"
    i18n_domain="{{ package_name }}">

  <subscriber
      for="{{ subscriber_for }}
           {{ subscriber_event }}"
      handler=".{{ subscriber_handler_name }}.handler"
      />

</configure>
