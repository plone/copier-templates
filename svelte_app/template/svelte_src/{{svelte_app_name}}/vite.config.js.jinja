import { defineConfig } from "vite";
import { svelte } from "@sveltejs/vite-plugin-svelte";

export default defineConfig({
  plugins: [svelte({
    compilerOptions: {
      customElement: {{ 'true' if svelte_app_custom_element else 'false' }},
    },
  })],
  build: {
    outDir: "../../src/{{ package_folder }}/svelte_apps/static/{{ svelte_app_name }}",
    emptyOutDir: true,
  },
});
