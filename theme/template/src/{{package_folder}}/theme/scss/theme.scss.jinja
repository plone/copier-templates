/* {{ theme_name }} theme — SCSS entry point.
 *
 * Import the design tokens defined in _variables, then layer your
 * component overrides on top. Compile with `npm run build`.
 */

@import "variables";

body {
  font-family: $font-family-base;
  color: $secondary;
}

a {
  color: $link-color;
}
